# Copy to keystore.properties at the repo root. Do not commit.
# Paths are resolved relative to the app/ module unless absolute.

storeFile=C:/path/to/upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
